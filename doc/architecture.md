# 🏛️ Swimmy Architecture: Lisp/Rust Responsibility Division

**Version**: V7.9 (Graham Critique #2)  
**Date**: 2026-01-02

---

## 設計原則: 「賢い脳、愚直な体」

```
┌─────────────────────────────────────────────────────────────┐
│  LISP BRAIN (Governance)                                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  • Constitution (judge-action)                              │
│  • Risk Management (daily loss, equity thresholds)          │
│  • Strategy Selection (regime-based filtering)              │
│  • Meta-decisions (when to trade, when to stop)             │
│  • State Management (PnL, positions, history)               │
└─────────────────────────────────────────────────────────────┘
                         ↑ Commands ↓ Proposals
┌─────────────────────────────────────────────────────────────┐
│  RUST BODY (Execution)                                      │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  • Market Data Processing (tick handling)                   │
│  • Signal Generation (NN, LSTM, PPO predictions)            │
│  • Order Execution (MT5 communication)                      │
│  • Reflex Arc (emergency crash detection)                   │
│  • Backtesting Engine (simulation)                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 責務分担表

| 機能 | Lisp | Rust | 理由 |
|------|:----:|:----:|------|
| **憲法審査** | ✅ | ❌ | ビジネスロジックは柔軟な言語で |
| **日次損失管理** | ✅ | ❌ | リスク判断は集中管理 |
| **戦略選択** | ✅ | ❌ | 高レベル意思決定 |
| **市場データ処理** | ❌ | ✅ | 高速処理が必要 |
| **NN/LSTM予測** | ❌ | ✅ | 計算集約型 |
| **MT5通信** | ❌ | ✅ | 低レイテンシ要求 |
| **緊急停止** | ⚠️ | ✅ | Reflex Arc (脊髄反射) |
| **バックテスト** | ❌ | ✅ | 大量計算 |

⚠️ = Lisp が判断、Rust が実行

---

## 通信プロトコル

### Rust → Lisp (提案)
```json
{
  "action": "BUY",
  "agent": "guardian-v1",
  "pnl": 15000,
  "equity": 1015000,
  "regime": "TRENDING",
  "strategy_type": "TREND"
}
```

### Lisp → Rust (判決)
```json
{
  "type": "command",
  "verdict": "APPROVED",
  "reason": "Constitution Respected. DD: 5000 < Limit: 50000"
}
```

---

## Graham's Validation Checklist

- [x] Lisp が**判断**し、Rust が**実行**する
- [x] ビジネスロジックは Lisp に集中
- [x] 計算集約型タスクは Rust に集中
- [x] 緊急時は Rust が自律的に行動 (Reflex Arc)
- [x] 通信は JSON Schema で型安全
