# 🦅 Expert Panel Report (Consult)

**Date:** 2026-02-10
**Leader:** Elon Musk
**Mode:** consult
**Trigger:** 「EV-First実装の cost_pips 設計（固定/動的/既存max-spread）」
**Purpose:** Pattern Similarity Gate を EV 一貫で運用するためのコストモデル確定
**Constraints:** ZMQはS式のみ／Pattern Similarity Gateは前段フィルタ／データ正本はData Keeper／H1+足確定時に評価
**Success Criteria:** EV判定の単位が一貫し、OOS/CPCVで劣化せず、運用コストが増えすぎない
**Assumptions:** bid/ask は実行時に取得可能、ティック由来VAPはData Keeperに保存可能
**Uncertainties:** pip-size の正規化が未定義、スプレッド履歴の粒度、swapの扱い

## 🏛️ 常設顧問の意見
### Taleb: 「Option3は“見た目だけ保守的”で最も危険」
- `*max-spread-pips*` をそのまま cost に使うのは **通貨ペアのpip単位が未確定**なままの見せかけ安全策。EURUSD等では桁違いの誤差になり得る。`src/lisp/school/school-constants.lisp:36-39`, `src/lisp/school/school-execution.lisp:214-222`
- Option2（動的cost）は正しいが、**pip-size定義**と**ask-bid→pip変換**がないと破綻する。`src/lisp/school/school-pip-audit.lisp:33-48`
- 選択肢（トレードオフ）:
  - **Option1**: 安全側の近似。短期で動くが過度に保守的になりがち。
  - **Option2**: もっとも正しいが、pip-size設計とデータ整備が必須。
  - **Option3**: 早いが、単位混在で“静かな破滅”の種。

### Graham: 「EVに本気なら、安易な近似は捨てろ」
- EV一貫を掲げている以上、**コスト単位の統一**は最低条件。固定costでもよいが、**根拠**が必要。`docs/plans/2026-02-10-pattern-similarity-gate-design.md:81-88`, `docs/llm/SPEC.md:63-65`
- Option1なら「ペア別固定cost」を**監査可能な根拠**（平均スプレッド）で固定すべき。

### Naval: 「運用負債を増やさない方法を選べ」
- Option2は正しいが、**データ収集/変換の運用負債**が増える。`src/lisp/school/school-db-swap.lisp:5-15`
- Option1は負債が低く、**検証サイクルが早い**。まずはOption1で検証し、勝ち筋が見えたらOption2へ移行。

### Jim Simons: 「数値の単位が曖昧なら、全てが嘘になる」
- SL/TPやspreadが“pips”名義だが実装は**価格差のまま**。EVの単位系が揺れる。`src/lisp/school/school-execution.lisp:214-222`, `src/lisp/school/school-constants.lisp:36-39`
- Option2にするなら、pip-sizeを明示し、すべてを同一単位で記録・検証すべき。

## 💻 技術パネルの意見
### Fowler: 「境界を1つ決めろ」
- cost計算は **1か所に集約**すべき。今は `*max-spread-pips*` と SL/TP の単位が分散している。`src/lisp/school/school-constants.lisp:36-39`, `src/lisp/school/school-execution.lisp:214-222`
- Option2なら、**pip-sizeテーブル + cost計算関数**を専用モジュール化するのが筋。

### Hickey: 「シンプルさを壊すな」
- 既に pip-value（損益換算）と “pips” 名のSL/TPが併存している。ここにさらに“cost_pips”を増やすなら、**意味の衝突を消せ**。`src/lisp/school/school-pip-audit.lisp:33-61`, `src/lisp/school/school-execution.lisp:214-222`
- Option1は単純だが、**命名と単位の明確化**がなければ意味がない。

### Uncle Bob: 「テスト不能なコストは仕様違反」
- Option2はユニットテストで `ask-bid → pip` が検証できる。Option3は検証不能で“暗黙の正しさ”に頼る。`src/lisp/school/school-pip-audit.lisp:33-61`
- 反証: ただし Option2 でも **pip-size定義が間違っていれば**テストは偽の安心を与える。

## 🚀 ビジョナリーの意見
### Ng: 「Option2が正しいが、データ設計が先」
- 動的costのために必要なデータは **bid/askの正規化**とpip-size。学習/検証の入力と同じ単位に揃えないとEVが崩壊する。`docs/plans/2026-02-10-pattern-similarity-gate-design.md:81-88`

### López de Prado: 「不正確なコストは過学習の温床」
- コストが過小に見積もられると、**OOS/CPCVで即死**する。Option3は最もそのリスクが高い。`docs/llm/SPEC.md:63-65`
- Option1は保守的で悪くないが、**検証時は複数シナリオ**を回すべき。

### Gene Kim: 「運用の観点ではOption1→2が現実的」
- まずOption1で回し、コストに敏感な箇所を見極めた後でOption2に移行するのが運用的に安全。
- Option2をいきなりやるなら、**運用runbookと監視項目**が必要。

## 🚀 Musk's Decision (Final)
> 「**結論: Option1で短期検証 → Option2で本番化。**
> Option3は却下。単位系が曖昧なままEVを語るのは欺瞞だ。」

## Actionable Items
1. **pip-sizeテーブルの設計**（通貨ペア別、ask-bid→pip変換の定義）: `src/lisp/school/school-constants.lisp:36-39`, `src/lisp/school/school-execution.lisp:214-222`
2. **cost計算関数の単一化**（EV評価と実行の双方で同じ関数を使用）: `src/lisp/school/school-pip-audit.lisp:54-61`
3. **Option1用の固定cost表を追加**（ペア別の保守値を明文化）: `src/lisp/school/school-pip-audit.lisp:33-48`
4. **検証シナリオの明文化**（低/中/高コストでOOS/CPCVを回す）: `docs/plans/2026-02-10-pattern-similarity-gate-design.md:81-88`
