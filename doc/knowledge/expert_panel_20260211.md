# 🦅 Expert Panel Report (Critique)

**Date:** 2026-02-11  
**Leader:** Elon Musk  
**Mode:** critique  
**Trigger:** 「Sharpe は高いのに昇格しない。ライブはまだか？」（2026-02-11 18:34 JST 再起動後も A=0/S=0）

## 🏛️ 常設顧問の意見
### Taleb: 「いまの設計は“静かな破滅”を起こす」
- A閾値を `WR>=38%` に緩和したのに、OOS結果適用側の判定が `WR>=43%` のまま残っている。これは“仕様変更したつもりで実は変わってない”最悪の不一致。`src/lisp/school/school-rank-system.lisp:25` と `src/lisp/school/school-validation.lisp:621`
- OOS成功件数が「A昇格成功」に見えるが、実装は rank/status 無視で単に `oos_sharpe>=min` を数えている。墓場も成功に見える。`src/lisp/school/school-db-oos.lisp:108`

### Graham: 「昇格ファネルを1本化しろ」
- 「候補抽出」「OOS投入」「昇格判定」が重複・分散していて、どこで落ちたか説明しにくい。`run-b-rank-culling-for-category` と `run-b-rank-culling` が別ロジックで同時に投げている。`src/lisp/school/school-rank-system.lisp:369`, `src/lisp/school/school-rank-system.lisp:494`
- まずは “A候補” の定義を一箇所に寄せないと、改善しても現場が追従できない。

### Naval: 「自動化はノイズを消してから」
- 現状は `Sharpe>=A閾値` だけで全BにA検証を投げるため、失敗ログが大量発生して運用判断を汚染する。`src/lisp/school/school-rank-system.lisp:496`
- 自動化の前に「検証対象の前フィルタ」を入れて負荷とノイズを削減すべき。

### Jim Simons: 「数理的に整合していない」
- B→A候補スコアが `Sharpe + 0.1*PF` 固定で、WR/DD/Expectancy を反映しない。A基準に一致しない最適化をしている。`src/lisp/school/school-rank-system.lisp:379`
- 指標の最適化対象と昇格基準がズレている限り、昇格率は上がらない。

## 💻 技術パネルの意見
### Fowler: 「重複ロジックがバグを生んでいる」
- A基準が `check-rank-criteria` と `meets-a-rank-criteria` で二重定義され、今回の不整合を作った。`src/lisp/school/school-rank-system.lisp:88`, `src/lisp/school/school-validation.lisp:612`
- さらに、OOS結果受信時の再昇格導線が `meets-a-rank-criteria` に依存し、旧閾値を温存してしまう。`src/lisp/school/school-validation.lisp:523`

### Hickey: 「シンプルにするには“意味の一貫性”が必要」
- `owners_guide` と実装・ナラティブの閾値文言がズレると、運用者が正しい判断をできない。`doc/owners_guide.md:193` と `src/lisp/school/school-narrative.lisp:375`
- システムは動いているが、意味論が壊れている。

### Uncle Bob: 「テストはあるが、壊れる場所を守っていない」
- 新規テストで `check-rank-criteria` 側は守られているが、実際の昇格導線で使う `meets-a-rank-criteria` の閾値整合テストが無い。`src/lisp/tests.lisp:3037`
- “通るテスト”より“実運用の分岐を固定するテスト”が不足している。

## 🚀 ビジョナリーの意見
### Ng: 「学習以前にラベルが壊れている」
- OOS success を rank無関係に集計しているため、運用KPIが誤ラベル化される。改善施策の効果測定が歪む。`src/lisp/school/school-db-oos.lisp:99`

### López de Prado: 「選抜バイアスが強すぎる」
- 候補選抜が昇格目的と非整合（Sharpe偏重）なので、OOS/CPCVで通る母集団を意図的に削っている。`src/lisp/school/school-rank-system.lisp:379`
- OOS/CPCVは後段で厳格なのに、前段で“間違った母集団”を作っている。

### Gene Kim: 「運用ドキュメントと真実のズレが事故を呼ぶ」
- `SYSTEM_ARCHITECTURE` は deprecate されており、参照導線が分断されている。`doc/SYSTEM_ARCHITECTURE.md:1`
- さらに report には Source Drift を出すだけで、自動で保護動作に入らない。`src/lisp/school/school-db-stats.lisp:85`

## 🚀 Musk's Decision (Final)
> 「やることは3つだけ。  
> 1) **閾値ロジックを単一化**して、今回のような“変更したつもり”事故を止める。  
> 2) **KPIを真実化**して、OOS成功と昇格成功を分離して見せる。  
> 3) **候補抽出をA/S基準準拠**に再設計する。  
> 逆に、PFやWRを場当たりでさらに下げるな。基準緩和ではなく、導線不整合を直せ。」

## Actionable Items
1. **A基準を単一関数化**: `meets-a-rank-criteria` を廃止し `check-rank-criteria` に統合、OOS結果導線も同一判定を使用。  
   参照: `src/lisp/school/school-validation.lisp:612`, `src/lisp/school/school-rank-system.lisp:88`
2. **即時バグ修正**: `meets-a-rank-criteria` の WR閾値を `get-rank-criteria :A` 参照へ変更（ハードコード排除）。  
   参照: `src/lisp/school/school-validation.lisp:621`
3. **候補抽出の再設計**: `run-b-rank-culling` の全B一括A検証（Sharpe only）を削除し、A基礎条件を満たす候補のみ投入。  
   参照: `src/lisp/school/school-rank-system.lisp:494`
4. **選抜スコアを昇格目的に整合**: `Sharpe+0.1*PF` を廃止し、A基準に沿った複合スコア（WR/DD/Expectancy含む）へ。  
   参照: `src/lisp/school/school-rank-system.lisp:379`
5. **メトリクス分離**: `oos_status` に `oos_pass_count` と `a_promoted_count` を別表示（rank別集計）。  
   参照: `src/lisp/school/school-db-oos.lisp:99`
6. **レポート文言の実装同期**: Evolution Report のA条件説明（WR43表記）を現行ロジックに同期。  
   参照: `src/lisp/school/school-narrative.lisp:375`, `doc/owners_guide.md:193`
7. **運用ガード**: `report-source-drift` 検出時に昇格処理を read-only モードへ切替える保護フラグを追加。  
   参照: `src/lisp/school/school-db-stats.lisp:85`

## やらないこと
- PF/WR閾値の追加緩和（場当たりな基準緩和）
- 「OOS success が多いから問題なし」という解釈
- ドキュメントだけ直してロジック二重定義を放置
