# 🦅 Expert Panel Report

**Date:** 2026-02-02  
**Leader:** Elon Musk  
**Trigger:** OOS自動審査が動かない懸念への確認と改善提案

## 🏛️ 常設顧問の意見
### Taleb
- `src/lisp/dsl.lisp:155-186` で OOS Sharpe の初期値が `0.0` のまま。ゼロが「既に評価済み」と誤解され、実際には未知なのに再検証が永遠に走らないリスクがある。Ruin 方向のサイレント失敗。`nil/NULL` を未評価の一意表現にせよ。
- `src/lisp/school/school-db.lisp:133-161` でも `or (strategy-oos-sharpe strat) 0.0` を保存しており、DB復元で「ゼロ＝既知」になる。リスク管理視点で、未評価をゼロに潰すのは致命的。

### Graham
- `src/lisp/school/school-validation.lisp:24-69` は OOS送信が 600 秒クールダウンのみで、送信失敗・キュー滞留の観測手段が無い。スケールするほど「静かな失敗」が溜まる。軽量メトリクスとダッシュボードが必要。

### Naval
- OOS審査の自動化レバレッジが低い。`maybe-request-oos-backtest` の送信成否をどこにも蓄積しておらず、夜間に止まっても気づけない。最小限の「1行サマリ」を Discord/Prometheus へ送るだけで運用コストが下がる。

### Jim Simons
- OOS判定が単発70/30分割のみ (`school-validation.lisp:50-69`)。時間非定常なFXでは分割1回は統計的に弱い。少なくとも Rolling/WFV 結果を併置し、偽陽性率を数値で出せ。

## 💻 技術パネルの意見
### Fowler
- `school-rank-system.lisp:228-241` の `normalize-oos-defaults` は良いが、`run-rank-evaluation` 以外の入口（手動昇格・再評価ジョブ）では呼ばれない。横断関心事をサービス層へ抽出し、再利用可能な OOS 正規化ユーティリティに。

### Hickey
- 全体がグローバル変数 (`*oos-pending*`, `*strategy-knowledge-base*`) に強く依存し、副作用が散らばる。未評価状態を `nil` にするような「値の意味」をデータ主導で定義し、関数は純粋化せよ。

### Uncle Bob
- OOS未評価時に送信するテストが存在しない。`tests/engine-tests.lisp` / `src/lisp/tests/school-split-tests.lisp` に最小のユニットテストを追加し、ゼロ初期値を `nil` に変えた際の回帰を防げ。

## 🚀 ビジョナリーの意見
### Ng
- データファイル存在チェックのみで品質を判定していない (`school-validation.lisp:53-59`)。ファイルサイズ0や欠損列を検知する軽量スキーマ検証を入れろ。ML的には入力品質監視が必須。

### López de Prado
- OOS Sharpeしきい値は固定0.3 (`school-validation.lisp:15-16`)。世代や市場 regime で動的に負荷を変えるべき。せめて世代別ベースラインを関数化し、過剰フィットを抑えろ。

### Gene Kim
- 運用視点で「OOS待ち件数」「最後に成功したOOS時刻」のヘルスチェックが無い。`task.md` や監視に組み込み、SREが一目で気付けるようにせよ。

## 🚀 Musk's Decision (Final)
> 「未評価をゼロで潰すのを直ちに止める。OOSの送信・待ち・結果を可視化し、データ品質検証を最低限入れる。CFDではスピードが命だが、見えないバグはもっと致命的だ。」

## Actionable Items
1. `dsl.lisp` の `oos-sharpe` 初期値を `nil` にし、`defstrategy` も `nil` に統一。  
2. `school-db.lisp` で `oos_sharpe` をそのまま保存（NULL許容）し、読み込みは `numberp` の場合のみ設定。  
3. `school-validation.lisp` に「OOS送信・待ち件数・最終成功時刻」をDiscord/Prometheusに出す簡易メトリクスを追加。  
4. `school-rank-system.lisp` の `normalize-oos-defaults` を手動昇格/再評価エントリでも呼べるヘルパーに抽出。  
5. ユニットテスト追加：OOS未評価で送信されること、ゼロ初期値を`nil`に正規化することを確認。  
6. データファイルの軽量スキーマ検証（ヘッダ・件数>0・必須列）を `run-oos-validation` 前に実施。  
