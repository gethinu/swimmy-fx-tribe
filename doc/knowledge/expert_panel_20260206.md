# 🦅 Expert Panel Report (Consult)

**Date:** 2026-02-06
**Leader:** Elon Musk
**Mode:** consult
**Trigger:** BACKTEST_RESULT のトレード一覧キーを `trades` 維持+`trade_list` 追加にするか、`trades` を一覧に置き換えて `trades_count` を新設するかの判断
**Purpose:** 後方互換を壊さずにトレード一覧を返せるインターフェースを決める
**Constraints:** 既存の BACKTEST_RESULT 消費ロジックと監視指標を壊さないこと。内部ZMQはS式のみ。最小のコード変更で運用リスクを下げる。
**Success Criteria:** 既存の `trades`（件数）を使う箇所が無改修で動き続け、トレード一覧を新規に取得できる。
**Assumptions:** `trades` は現在「件数」を意味し、監視/ログ/テストで数値として扱われている。
**Uncertainties:** 既存の BACKTEST_RESULT 受信経路（JSON/S式）の全消費者の実態、そして大量トレード時のペイロード制限。

## 🏛️ 常設顧問の意見
### Taleb: 「壊れる場所を増やすな。既存契約を守れ」
- 問題点: `trades` を配列に変更すると、内部監視が「数値」として扱う場所が静かに壊れる。`*backtest-recv-last-trades*` は数値を前提にログやステータスに出力している。`src/lisp/core/message-dispatcher.lisp:195-229` で明示的に数値化している点が脆弱性。 
- 選択肢:
  1. **`trades` は件数のまま固定**し、詳細は `trade_list` を別キーで追加（推奨）。互換が最強。
  2. `trades` を配列に置き換え、`trades_count` を新設。API潔癖だが既存契約破壊。
  3. `trades` は件数、`trades_count` も併記して二重化（冗長）。
- 反証: 「新キー増殖はスキーマが肥大化する」という反対意見は正しいが、**互換破壊の破滅確率**に比べれば安い。 
- 参照: `src/lisp/core/message-dispatcher.lisp:195-229`

### Graham: 「少しでも複雑になるなら、移行コストを減らせ」
- 問題点: 既存ドキュメントは `trades` を件数として明記しており、ここを変えると全ユーザーの精神モデルが崩れる。`docs/llm/INTERFACES.md:220-228`。
- 選択肢:
  1. `trade_list` 追加で小さく前進（推奨）。
  2. `trades` を配列に変えて「美しい」APIにするが、既存ドキュメント修正が大きい。
- トレードオフ: 1は互換性◎だがキーが増える。2は見た目はきれいだが波及範囲が大きい。
- 参照: `docs/llm/INTERFACES.md:220-228`

### Naval: 「レバレッジは維持費を下げる方向へ」
- 問題点: `trades` を配列に変えるとテスト修正やデバッグが増えて、運用コストが上がる。`src/lisp/tests.lisp:82-120` は `trades` を数値として期待している。
- 選択肢:
  1. `trade_list` 追加で既存テストを壊さない（推奨）。
  2. `trades` を配列に変更し、テスト全面修正 + 移行サポート。
- トレードオフ: 1は最小コスト、2は美観だが負債増。
- 参照: `src/lisp/tests.lisp:82-120`

### Simons: 「統計系列は壊すな」
- 問題点: `trades` は履歴系列の長さとして統計的に重要。数値の意味を変えるとメトリクスやログの整合性が崩れる。`src/lisp/core/message-dispatcher.lisp:195-229`。
- 選択肢:
  1. `trades` = 件数固定、`trade_list` を別項目にする（推奨）。
  2. `trades` を配列にし、件数を `trades_count` として再計算。
- トレードオフ: 1は統計互換を守る、2はAPIの再学習コストを強いる。
- 参照: `src/lisp/core/message-dispatcher.lisp:195-229`

## 💻 技術パネルの意見
### Fowler: 「公開契約の名前は壊すな」
- 問題点: インターフェース定義は公開契約。`trades` の意味変更は破壊的。`docs/llm/INTERFACES.md:220-228`。
- 選択肢:
  1. **新キー追加**（`trade_list`）で後方互換を維持（推奨）。
  2. `trades` の意味変更 + 互換層追加（複雑）。
- トレードオフ: 1は互換性◎、2は二重変換が増える。
- 参照: `docs/llm/INTERFACES.md:220-228`

### Hickey: 「意味のある名前、意味の安定」
- 問題点: `trades` の意味が「件数」から「配列」に変わるのは、値の意味が揺れるアンチパターン。
- 選択肢:
  1. `trades` を件数のまま保持し、`trade_list` で表現を追加（推奨）。
  2. `trades` を配列に変更し、`trades_count` を新設。
- トレードオフ: 1は言語的な意味の安定を維持、2は意味が揺れ、読み手に負担。
- 参照: `docs/llm/INTERFACES.md:220-228`

### Uncle Bob: 「テストを壊すのは悪」
- 問題点: `trades` を配列にすると既存テストが壊れる。既存の `BACKTEST_RESULT` テストが数値前提で作られている。`src/lisp/tests.lisp:82-120`。
- 選択肢:
  1. `trade_list` 追加で既存テスト維持（推奨）。
  2. テスト全書き換えで意味変更に追従。
- トレードオフ: 1は最小変更、2はテストメンテコスト増。
- 参照: `src/lisp/tests.lisp:82-120`

## 🚀 ビジョナリーの意見
### Ng: 「データ量の爆発を避ける」
- 問題点: トレード一覧を返すとメッセージサイズが膨張する。まず互換性を守りつつ、サイズ制御を別設計で扱うのが合理的。
- 選択肢:
  1. `trade_list` をオプションとして追加し、サイズ制御は別で決める（推奨）。
  2. `trades` を配列にしてしまい、常に肥大化リスクを背負う。
- トレードオフ: 1は可搬性が高い、2はスケールで苦しむ。

### López de Prado: 「可観測性の連続性」
- 問題点: `trades` が件数でなくなると、今までのバックテスト統計が比較不能になる。
- 選択肢:
  1. `trades` は件数固定、`trade_list` で詳細を添付（推奨）。
  2. `trades` を配列化し、件数は再計算。
- トレードオフ: 1は時系列分析が連続、2は移行が必要。

### Gene Kim: 「運用で壊れない設計」
- 問題点: 運用ログは数値トレード数を前提に出力される。`src/lisp/core/message-dispatcher.lisp:140-147` の status 出力も影響。
- 選択肢:
  1. `trade_list` 追加で運用の継続性を保つ（推奨）。
  2. `trades` の意味変更に合わせて監視/ログ改修。
- トレードオフ: 1は安定運用、2は変更量増大。
- 参照: `src/lisp/core/message-dispatcher.lisp:140-147`

## 🚀 Musk's Decision (Final)
> 「`trades` は件数のまま固定し、トレード一覧は `trade_list` で追加する。互換性を壊さずに前進しろ。」

## Actionable Items
1. `docs/llm/INTERFACES.md` に `trade_list`（一覧）を追加し、`trades` は件数と明記する。`docs/llm/INTERFACES.md:220-228` に追記。
2. `src/lisp/core/message-dispatcher.lisp` の `trades` を引き続き数値として扱う前提を維持し、必要なら `trade_list` 受理は別処理で追加する。`src/lisp/core/message-dispatcher.lisp:195-229`。
3. 既存テストは維持し、`trade_list` 用の新規テストを追加する（必要なら）。`src/lisp/tests.lisp:82-120`。
