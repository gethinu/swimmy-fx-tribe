# 🦅 Expert Panel Report (Consult)

**Date:** 2026-02-06  
**Leader:** Elon Musk  
**Mode:** consult  
**Trigger:** Max Age Retirement をこのまま「廃棄/ソフトキル」でよいか、学習データ化すべきかの相談
**Purpose:** Max Age Retirement の処遇（廃棄・墓場学習・保留）を決め、学習品質と運用安定性の最適バランスを取る
**Constraints:** 墓場データの汚染回避、KB肥大化の回避、既存のランク/永続化設計（graveyard即時削除）との整合性維持
**Success Criteria:** Max Age 由来の戦略が「意図通りの状態」に遷移し、学習データがラベル汚染せず、運用指標/DB/KBの整合が崩れない
**Assumptions:** 年齢到達は必ずしも失敗ではない、Soft-Kill はクールダウン/棚上げの意味を持つ、graveyard は学習・回避パターンに影響する
**Uncertainties:** Max Age 到達戦略の実績分布、墓場学習がどの程度「寿命由来」を誤学習するか、Soft-Kill からの復帰運用が実際に行われているか

## 🏛️ 常設顧問の意見
### Taleb: 「寿命=失敗」と決めつけるな。だが“棚上げ”は見えない負債になる
Max Age Retirement は年齢条件だけで kill しており、失敗パターン保存も KB 除去も起きない。これは「壊れていないものを捨てる」か「腐敗させる」かの二択になっている。`src/lisp/school/school-breeder.lisp:365` `src/lisp/strategies/strategies.lisp:251` `src/lisp/school/school-rank-system.lisp:99`
選択肢:
1. Soft-Killは維持しつつ「Retired-Archive」を新設し、墓場とは別の“非失敗”保存に回す。学習汚染は避けられるが、アーカイブ運用の追加コストが出る。
2. Max Age でも `send-to-graveyard` へ送る。ただし reason ラベルで「age-retired」を識別し、学習側で重みを下げる。学習が強くなるが、汚染リスクが残る。

### Graham: 単純化しろ。「Soft-Killの宙ぶらりん」が一番危険
`kill-strategy` がランク/DB/墓場の正規経路を回避しているため、状態が増えているのに設計の単一性が崩れている。`src/lisp/strategies/strategies.lisp:251` `src/lisp/school/school-rank-system.lisp:99`
選択肢:
1. Max Age は “明示的に墓場 or 明示的に保持” の二択に統一。コードは単純化されるが、柔軟性は下がる。
2. `kill-strategy` を廃止し、必ず `ensure-rank` 経由に統一する。設計は整うが、既存の Soft-Kill 運用は変わる。

### Naval: レバレッジは「状態の透明性」から来る。今は暗黙状態だらけ
Soft-Kill は `strategy-status` だけを変え、DBや墓場に記録されない。Heartbeatは “active vs KB” を出すが、Soft-Kill の規模は見えない。`src/lisp/strategies/strategies.lisp:251` `src/lisp/engine/heartbeat.lisp:47`
選択肢:
1. Soft-Kill を維持するならメトリクス化（件数・理由・年齢）を強制。透明性は上がるが運用負荷が増える。
2. Soft-Kill をやめて graveyard or archive に一本化。観測は単純化されるが、短期の再評価フローは犠牲になる。

### Jim Simons: 検閲（censoring）が統計を歪める
Max Age は性能ラベルなしで切り捨てられる。これが墓場学習にも KB 活性にも反映されないのは、データ欠損だ。`src/lisp/school/school-breeder.lisp:365` `src/lisp/school/school-rank-system.lisp:154`
選択肢:
1. `save-failure-pattern` を age-retired 用に別系統で保存し、学習に“弱く”効かせる。統計の欠損を減らせるが、実装コストが増える。
2. Max Age でも性能指標が閾値未満のときのみ graveyard へ送る。合理的だが、閾値設計が新たなリスク。

## 💻 技術パネルの意見
### Fowler: ライフサイクルが分岐していて保守不能
ランク経由の正規フロー（`ensure-rank`）と、Soft-Kill の裏道が併存している。ここは設計上の分岐点であり、将来のバグ源。`src/lisp/strategies/strategies.lisp:251` `src/lisp/school/school-rank-system.lisp:99`
選択肢:
1. Max Age は `send-to-graveyard` に統一し、`kill-strategy` は削除。シンプルだが運用の柔軟性は落ちる。
2. `kill-strategy` を `retire-strategy` に改名し、永続化と明示的な状態管理（archive保存）を実装。設計は増えるが一貫性は上がる。

### Hickey: 「状態」と「意味」が混ざっている
`strategy-status` は :killed なのに rank は変わらない。意味が二重化している。`src/lisp/strategies/strategies.lisp:251` `src/lisp/school/school-rank-system.lisp:114`
選択肢:
1. 状態を rank だけに統一して意味を一本化。単純だが既存の status ロジックを整理する必要がある。
2. status を残すなら status→rank 変換規約を追加し、必ず永続化する。柔軟だが規約運用の負担が増える。

### Uncle Bob: テストがない意思決定は“壊れている”と同じ
Max Age の処遇が仕様かバグか不明。最低限、Max Age で墓場/アーカイブ/Soft-Kill のどれに行くかのテストが必要。`src/lisp/school/school-breeder.lisp:365` `src/lisp/strategies/strategies.lisp:251`
選択肢:
1. Max Age の行先を固定し、テストで固定する。品質は上がるが、運用変更に弱くなる。
2. ルールを設定ファイル化し、テストは“設定反映”を検証。柔軟だが設定ミスのリスクが増える。

## 🚀 ビジョナリーの意見
### Ng: 教師データのラベル汚染に注意
墓場は「失敗学習」用データと明記されている。Max Age を墓場に入れるなら“失敗”として誤学習する恐れがある。`src/lisp/school/school-rank-system.lisp:154` `doc/owners_guide.md:25`
選択肢:
1. “age-retired” の別ラベルで保存し、学習で重みを低くする。正確だが実装が増える。
2. 墓場には入れず、再評価プールへ移す。汚染は避けられるが学習シグナルは増えない。

### López de Prado: 市場は非定常。寿命で殺すのは検閲だ
年齢による除外はサンプル選択バイアスを生む。将来のレジームで再起動すべき戦略を消す可能性がある。`src/lisp/school/school-breeder.lisp:380`
選択肢:
1. “冷凍”アーカイブを作り、再評価で復活可能にする。柔軟だが運用コストが上がる。
2. Max Age の基準をパフォーマンス連動に変える。合理的だが設計が複雑になる。

### Gene Kim: オペレーション的には可観測性が足りない
Soft-Kill は KB 内に残るが、墓場にも DBにも反映されない。運用指標がズレる。`src/lisp/strategies/strategies.lisp:251` `src/lisp/engine/heartbeat.lisp:47`
選択肢:
1. Soft-Kill 件数/理由/年齢を telemetry に載せる。運用は楽だが実装が増える。
2. Soft-Kill を廃止して墓場 or アーカイブに統一。可観測性は上がるが柔軟性は下がる。

## 🚀 Musk's Decision (Final)
> 「“寿命で廃棄”は雑すぎる。Max Age は『失敗』とは限らない。だから墓場に直送はやめろ。だが Soft-Kill の宙ぶらりんも許さない。**Retired-Archive を作り、Max Age は必ずそこへ送る。** さらに学習用途には“age-retired”としてラベル付きで低ウェイト保存しろ。運用と学習の両方に整合を持たせる。」

## Actionable Items
1. Max Age Retirement の遷移先を `send-to-graveyard` ではなく `retire-to-archive` に統一する設計変更を作成（新設する場合は `strategy-status` と rank の整合規約を明文化）。`src/lisp/school/school-breeder.lisp:365` `src/lisp/strategies/strategies.lisp:251`
2. age-retired 専用の保存（例: `data/memory/retired.sexp` または `graveyard.sexp` に reason ラベル）を追加し、学習側で重みを低く扱う設計を決める。`src/lisp/school/school-rank-system.lisp:154`
3. Soft-Kill の可観測性を追加するか廃止するか、どちらかに統一する（メトリクス追加なら heartbeat/telemetry に “killed count” を出す）。`src/lisp/engine/heartbeat.lisp:47`

反証/対抗意見:
Max Age が“環境適応できない戦略の失敗”に近いなら、墓場に送って学習する方が合理的であり、アーカイブは単なる肥大化を招く可能性がある。`src/lisp/school/school-breeder.lisp:380`
