# 🦅 Expert Panel Report (Consult)

**Date:** 2026-02-08
**Leader:** Elon Musk
**Mode:** consult
**Trigger:** 「週末でトレードが動いていないため非相関スコアが N/A。仮で何か入れるべきか？」
**Purpose:** N/A の扱いを決め、通知の信頼性とオペレーションの混乱を抑える。
**Constraints:** 実データが無い週末/休場は必ず発生する。非相関はライブ `trade_logs` 由来のみ（設計上バックテストは非対象）。
**Success Criteria:** 1) 誤解を生まない通知 2) 意思決定に必要な情報が不足していることが明確 3) 監視負担を増やさない。
**Assumptions:** `calculate-noncorrelation-score` はデータ不足で `N/A` を返す設計。通知は昇格時に必ず送られる。 `strategy_daily_pnl` が空なら N/A は必然。 (src/lisp/school/school-dalio.lisp:137-204)
**Uncertainties:** 現場が「数値が無いと不安」という心理的要求を持つか。N/A 連発が運用上ノイズと見なされるか。

## 🏛️ 常設顧問の意見
### Taleb: 
- **主張:** 仮の数値は「偽の確実性」。破滅回避の観点では最悪。N/A を「不足」として出すのは健全。 (src/lisp/school/school-dalio.lisp:143-191)
- **選択肢:**
  1. **N/A維持 + 充足条件を明示**（例: `N=0/30`）: 真実性最優先。誤判定リスク最小。
  2. **推定値を出す**: 破滅リスク増。信頼性が崩れる。
- **対抗意見:** 「現場が数値を求める」という圧があるなら、値ではなく「不足理由の定量化」にすべき。

### Graham:
- **主張:** 意思決定に使えない数値は出すな。数値があることで錯覚が生まれる。
- **選択肢:**
  1. **通知内で不足理由と次回更新時刻を明示**: 使えない情報を明確化。
  2. **昇格通知と非相関スコア通知を分離**: スコアがある時だけ送る。
  3. **仮値を出す**: 監視のノイズ化。

### Naval:
- **主張:** 人間の注意は有限。N/A 連発は注意経済の浪費。
- **選択肢:**
  1. **N/Aを「Data pending」＋スコア生成条件を短く表示**: 認知負荷を低減。
  2. **通知を抑制**（スコアが算出可能な時だけ）: ノイズ低減だが昇格通知から情報が欠落。

### Simons:
- **主張:** 欠測は統計的事実。推定を入れるなら不確実性の幅を必ず表示せよ。
- **選択肢:**
  1. **N/A維持**（現行）: 統計的に最も誠実。
  2. **事前分布によるベイズ推定 + CI 表示**: 実装重い。誤解リスク大。
  3. **サンプルNが足りるまでスコア非表示**: 正しいが「沈黙」を許容する必要。

## 💻 技術パネルの意見
### Fowler:
- **主張:** 表示責務がロジックに直結している。理由コードと表示フォーマットを分離せよ。 (src/lisp/school/school-dalio.lisp:176-204)
- **選択肢:**
  1. **`calculate-noncorrelation-score` は `reason` を返し、UI側で理由を明示**: 可読性向上。
  2. **N/Aの代替値導入**: システム境界が曖昧になる。

### Hickey:
- **主張:** データが無いのに数値を出すのは「嘘」。システムの意味論が壊れる。
- **選択肢:**
  1. **N/A維持**（推奨）: 値の意味が保たれる。
  2. **推定値**: 意味論が破壊される。

### Uncle Bob:
- **主張:** 振る舞いはテストで保証すべき。N/Aや理由表示が仕様ならテストで固定する。 (src/lisp/tests.lisp:1920-1942)
- **選択肢:**
  1. **N/A時の文言を固定化し、テスト追加**: 仕様のブレを防ぐ。
  2. **仮値導入**: テスト負担増、バグ温床。

## 🚀 ビジョナリーの意見
### Ng:
- **主張:** 欠測は「データの欠如」ではなく「システム状態」。それを特徴として出すべき。
- **選択肢:**
  1. **`coverage` 指標（例: 0/30日）を追加**: モデル/人間の両方に理解可能。
  2. **推定値**: 学習データと乖離する恐れ。

### López de Prado:
- **主張:** 相関推定は小標本で最も不安定。N不足の仮値は誤情報。
- **選択肢:**
  1. **N/A維持 + `N` 表示**: 統計的に誠実。
  2. **ウィンドウ短縮（30→10日など）**: だが推定誤差増大。
  3. **シュリンク推定**: 設計と説明コストが増える。

### Gene Kim:
- **主張:** これは「観測可能性」の問題。N/Aの頻度は運用メトリクスにすべき。 (docs/plans/2026-02-06-noncorrelated-discord-notification-design.md)
- **選択肢:**
  1. **N/A発生率・`strategy_daily_pnl` 行数をテレメトリ化**: 原因の可視化。
  2. **通知抑制**: データ不足の状況が隠れる。

## 🚀 Musk's Decision (Final)
> 「**仮値は入れるな**。N/Aは正しい。だが**N/Aの理由を具体化**しろ。人間が次のアクションを取れる情報だけを足せ。」

## Actionable Items
1. **通知文言に `coverage` を追加**: 例 `非相関スコア: N/A (データ不足: 0/30日)`。`notify-noncorrelated-promotion` で `count` を埋める。 `src/lisp/school/school-dalio.lisp:176-199`
2. **不足理由の拡張**: `:insufficient-data` と `:insufficient-strategies` を明示的にテキスト化（現状は括弧のみ）。 `src/lisp/school/school-dalio.lisp:188-191`
3. **N/A率の観測**: `strategy_daily_pnl` 行数と N/A 発生をログに出し、週末/平日の差分を可視化。 `docs/plans/2026-02-06-noncorrelated-discord-notification-design.md`
4. **方針固定テスト**: N/A時の文言と理由をテストに固定し、仕様のブレを防止。 `src/lisp/tests.lisp:1920-1942`

# 🦅 Expert Panel Report (Critique)

**Date:** 2026-02-08
**Leader:** Elon Musk
**Mode:** critique
**Trigger:** フェーズ7まで進まないとレポートが発火しない件（進化ループ停止時の可観測性欠落）

## 🏛️ 常設顧問の意見
### Taleb:
監視が監視対象に依存しているのは「破滅リスクの増幅」だ。`phase-7-report` は進化ループの最後に埋め込まれており、Phase1〜6のどこかで詰まると報告が沈黙する。失敗時にこそ報告が必要なのに、最も脆弱なタイミングで沈黙する設計だ。`*last-report-time*` もメモリ内状態で永続性がなく、再起動で時間窓が崩れる。 `src/lisp/school/school-connector.lisp:102-118` `src/lisp/school/school-connector.lisp:152-187`

### Graham:
運用の“地面”が揺れる。既にスケジューラは「毎日23:00のレポート」を扱っているが、進化レポートだけ別のループに置かれている。運用負債と説明コストを増やすだけだ。成長や故障時に説明可能であるべき。 `src/lisp/core/scheduler.lisp:11-52` `src/lisp/school/school-connector.lisp:106-114`

### Naval:
レバレッジが足りない。`notify-evolution-report` は完結した関数なのに、呼び出しが進化ループ依存で“人間が手動で叩く”設計になっている。これは自動化の敗北だ。独立タイマー/サービス化で運用コストをゼロにすべき。 `src/lisp/school/school-narrative.lisp:321-325` `src/lisp/school/school-connector.lisp:106-114`

### Simons:
「真実のソースはSQLite」と定義しているなら、報告はDBスナップショットから独立に生成できるはずだ。にもかかわらず、ループ完走時しか出ないのは統計的に最悪の“欠測バイアス”を作る。さらに別系統のPythonレポートはファイル数を“真実”としており、ソースが分裂している。 `doc/owners_guide.md:195` `src/lisp/school/school-narrative.lisp:222-235` `tools/report_evolution.py:1-88`

## 💻 技術パネルの意見
### Fowler:
スケジューリングと進化オーケストレーションの責務が混在している。`start-evolution-service` の中で報告・ハートビートまでやっているのはSRP違反で、保守性が落ちる。既存のスケジューラに「進化レポート」を追加するのが正道だ。 `src/lisp/school/school-connector.lisp:152-196` `src/lisp/core/scheduler.lisp:54-69`

### Hickey:
時間依存のグローバル変数 `*last-report-time*` は静かなバグの温床だ。時間状態は外部化し、関数は「いつでも正しく動く」べき。報告は純粋なDBスナップショット生成に落とし、トリガーは外部タイマーに任せるべきだ。 `src/lisp/school/school-connector.lisp:102-118` `src/lisp/school/school-narrative.lisp:301-308`

### Uncle Bob:
テスト可能性が低い設計だ。`phase-7-report` は進化ループ内の副作用で、ループが止まった時の挙動をテストで保証できない。報告を独立関数としてスケジュールし、スモークテストで「進化停止でも報告が動く」ことを固定すべきだ。 `src/lisp/school/school-connector.lisp:106-118` `src/lisp/school/school-narrative.lisp:321-325`

## 🚀 ビジョナリーの意見
### Ng:
既にStructured Telemetryを導入しているのに、報告が進化ループ依存なのは観測性の設計ミスだ。報告はテレメトリの一部として独立に発火し、失敗時の状態を必ず記録すべき。 `doc/owners_guide.md:1-6` `src/lisp/school/school-connector.lisp:191-196`

### López de Prado:
欠測は最悪のバイアスだ。報告が「成功時にだけ出る」構造なら、統計的に使い物にならない。故障時の欠測こそ記録し、報告が出ていないこと自体をアラートにすべきだ。 `src/lisp/school/school-connector.lisp:106-118` `src/lisp/school/school-connector.lisp:194-195`

### Gene Kim:
これは“データプレーン”に“コントロールプレーン”を混ぜている問題だ。報告と監視は独立サービスであるべき。運用の基本は「落ちても観測は残る」。スケジューラかsystemd timerで報告を分離するのが最短だ。 `src/lisp/core/scheduler.lisp:13-69` `src/lisp/school/school-connector.lisp:106-118`

## 🚀 Musk's Decision (Final)
> 「**レポートは進化ループから切り離す**。フェーズ7まで進まないと発火しない設計は捨てる。監視は独立に動く。“報告が止まった”という事実自体がアラートになる仕組みを入れる。ソースはDBに統一し、二重系は整理する。」

## Actionable Items
1. 進化ループから報告発火を分離し、独立スケジュールで `notify-evolution-report` を1時間ごとに実行する。 `src/lisp/school/school-narrative.lisp:321-325` `src/lisp/core/scheduler.lisp:23-69`
2. レポート/ハートビートの鮮度監視を追加し、「古い」状態を明示的にアラート化する。 `src/lisp/school/school-connector.lisp:184-196` `src/lisp/school/school-connector.lisp:106-118`
3. レポートの真実ソースをDBに統一し、filesystemカウント方式の報告は統合または退役させる。 `doc/owners_guide.md:195` `tools/report_evolution.py:1-88` `src/lisp/school/school-narrative.lisp:222-235`
4. 進化ループ停止時でも報告が生成されることをスモークテストで固定する。 `src/lisp/school/school-connector.lisp:106-118` `src/lisp/school/school-narrative.lisp:321-325`

# 🦅 Expert Panel Report (Critique)

**Date:** 2026-02-08
**Leader:** Elon Musk
**Mode:** critique
**Trigger:** 「construct-global-portfolio による A→S 昇格が CPCV を通らない。これは逸脱か？」

## 🏛️ 常設顧問の意見
### Taleb:
Sは「ライブ実行許可」のはずなのに、CPCVを通さずにSへ昇格する経路が存在する。これは“見えない破滅リスク”を増幅する設計だ。Sの意味を壊すなら、Sを名乗るな。`construct-global-portfolio` のドラフトは別ランク/別フラグに隔離し、SはCPCVゲートのみ通すべきだ。 `src/lisp/school/school-portfolio.lisp:12` `src/lisp/school/school-portfolio.lisp:31` `doc/owners_guide.md:176` `docs/llm/SPEC.md:101`

### Graham:
ドキュメントでは `B → (OOS) → A → (CPCV) → S` と定義されている。にもかかわらず、Aが“ドラフト”でSに化けるのは「会社の説明不能ポイント」を生む。これは信用毀損だ。仕様を守るか、仕様を改めるか、どちらかに決めろ。 `doc/owners_guide.md:176` `doc/owners_guide.md:181` `src/lisp/school/school-portfolio.lisp:31`

### Naval:
自動化の価値は“意味の一貫性”にある。Sを「CPCV済み」と「ドラフト選抜」が共用する時点でレバレッジは崩れる。Sを二義化せず、`S_CANDIDATE` か `GLOBAL_TEAM` の別状態に切り出せ。 `src/lisp/school/school-portfolio.lisp:31` `src/lisp/school/school-rank-system.lisp:22`

### Simons:
Sharpeだけのドラフト昇格は“統計の嘘”。CPCVは過学習の防波堤で、Sの意味を担保する。いまのままではSが「最もデータリーキーな集合」になり、評価が崩壊する。 `src/lisp/school/school-portfolio.lisp:25` `src/lisp/school/school-validation.lisp:331` `docs/llm/SPEC.md:101`

## 💻 技術パネルの意見
### Fowler:
昇格ロジックが複数箇所に分散し、整合性が壊れている。`message-dispatcher` の CPCV経路と、`construct-global-portfolio` のドラフト経路が並列で、単一のルールとして管理されていない。Sへの昇格は必ず単一関数でゲートすべきだ。 `src/lisp/core/message-dispatcher.lisp:363` `src/lisp/school/school-portfolio.lisp:31` `src/lisp/school/school-rank-system.lisp:106`

### Hickey:
「S」という語が意味するものがぶれている。意味論が壊れたシステムは修正不能になる。Sは“CPCV済み”で固定し、ドラフトは別の値/タグに切り分けろ。 `docs/llm/SPEC.md:101` `src/lisp/school/school-portfolio.lisp:31`

### Uncle Bob:
テストで守られていない。A→Sの抜け道があるなら、それは“仕様違反を許すテスト欠落”だ。`construct-global-portfolio` から `ensure-rank :S` が起きた時にCPCV未完了なら失敗するテストを追加しろ。 `src/lisp/school/school-portfolio.lisp:31` `src/lisp/school/school-rank-system.lisp:52`

## 🚀 ビジョナリーの意見
### Ng:
Sを“教師信号”として使うなら、誤ラベルは学習の毒だ。CPCV未検証のSが混ざれば、将来の戦略生成にも誤学習が入る。昇格理由（CPCVかドラフトか）を必ずテレメトリに出して監査可能にせよ。 `src/lisp/school/school-portfolio.lisp:31` `src/lisp/core/message-dispatcher.lisp:363`

### López de Prado:
相関・多様性を見たつもりでも、CPCVなしのSは“バックテストの勝者呪い”に直撃する。CPCVを飛ばすならSではない。これは統計的な逸脱だ。 `src/lisp/school/school-portfolio.lisp:25` `doc/owners_guide.md:176`

### Gene Kim:
運用視点ではS＝「ライブ許可」の合意が崩れる。オンコールは原因解析不能になる。S昇格の理由を必ず通知に付与し、ルール違反が起きたら即アラートに落ちる仕組みにすべきだ。 `src/lisp/school/school-portfolio.lisp:31` `src/lisp/school/school-dalio.lisp:184`

## 🚀 Musk's Decision (Final)
> 「**これは逸脱だ。** SはCPCVを通した“Verified Elite”に固定する。ドラフトは別の状態に切り出し、Sの意味を一切汚さない。」

## Actionable Items
1. `ensure-rank` で **S昇格時にCPCV未完了なら拒否** するガードを追加する（例外は明示的フラグのみ）。 `src/lisp/school/school-rank-system.lisp:52` `src/lisp/school/school-rank-system.lisp:106`
2. `construct-global-portfolio` の昇格先を **`S_CANDIDATE`（新設）または `A` 維持** に変更し、SはCPCV専用に戻す。 `src/lisp/school/school-portfolio.lisp:31`
3. 昇格通知に **昇格理由（CPCV/ドラフト）** を必ず表示し、逸脱を即時検出可能にする。 `src/lisp/school/school-dalio.lisp:184`
4. 「A→SがCPCV無しで成立しない」テストを追加し、抜け道を固定的に潰す。 `src/lisp/school/school-portfolio.lisp:31` `src/lisp/school/school-rank-system.lisp:52`
5. ドキュメントのS定義と実装を一致させる（docs/llm/SPEC と owners_guide の整合性確認）。 `docs/llm/SPEC.md:96` `doc/owners_guide.md:176`

# 🦅 Expert Panel Report (Critique)

**Date:** 2026-02-08  
**Leader:** Elon Musk  
**Mode:** critique  
**Trigger:** Phase2（V2）未実装のまま報告に出ている件（`Phase2 EndTime: N/A`）

## 🏛️ 常設顧問の意見
### Taleb:
Phase2を“存在する前提”で報告に出すのは偽の確実性だ。未実装の検証ステージを表示すること自体が破滅リスクを隠蔽する。Phase2の発火は `request-backtest-v2` の `phase="phase2"` でしか記録されないのに、実行経路が無い。 `src/lisp/school/school-backtest-v2.lisp:41-105` `src/lisp/school/school-narrative.lisp:289-324`

### Graham:
仕様は `B→(OOS)→A→(CPCV)→S` と書いてある。Phase2が本当に必要なら、実装と運用に接続すべきで、無いならドキュメントから消せ。嘘の指標は信用毀損だ。 `doc/owners_guide.md:166-181` `src/lisp/school/school-backtest-v2.lisp:100-105`

### Naval:
Phase2の状態がメモリ変数 `*phase2-last-end-unix*` だけに依存していて、再起動で消えるのは自動化の敗北。永続化か、明示的に「Inactive」として扱うべきだ。 `src/lisp/core/globals.lisp:112-113` `src/lisp/school/school-narrative.lisp:289-324`

### Simons:
OOS検証とPhase2(V2)の二重系が混在し、どれが真の検証か不明瞭。統計的には「未実施の検証を実施済みに見せる」ことが最大のリスクだ。 `src/lisp/school/school-validation.lisp:244-299` `src/lisp/school/school-backtest-v2.lisp:100-105`

## 💻 技術パネルの意見
### Fowler:
Phase2は定義されているが呼び出しが無い“死んだ経路”。さらにテストは表示のみを保証し、実行経路を保証していない。これは設計の負債だ。 `src/lisp/school/school-backtest-v2.lisp:100-105` `src/lisp/tests/school-split-tests.lisp:461-476`

### Hickey:
`Phase2 EndTime` がN/Aのままなら、そのフィールドは意味を持たない。意味のない語彙は削除するか「Inactive」と明示して意味論を守れ。 `src/lisp/school/school-narrative.lisp:289-324`

### Uncle Bob:
Phase2が動作しなくてもテストが通るのはバグ。少なくとも「Phase2が実行されたら end_time が更新される」統合テストが必要だ。 `src/lisp/tests/school-split-tests.lisp:461-476` `src/lisp/school/school-backtest-v2.lisp:41-105`

## 🚀 ビジョナリーの意見
### Ng:
Phase2が未接続なら学習信号が欠損し、OOSとPhase2の意味が混線する。状態は「実行済み/未実行/無効」を明示して再現性を担保すべきだ。 `src/lisp/school/school-validation.lisp:244-299` `src/lisp/school/school-narrative.lisp:289-324`

### López de Prado:
検証ステージの空欄は過学習の温床だ。Phase2を使わないなら削除し、使うなら厳格に接続して監査可能にせよ。 `src/lisp/school/school-backtest-v2.lisp:100-105` `doc/owners_guide.md:166-181`

### Gene Kim:
“N/A”は故障なのか正常なのか判別できない。運用指標は状態遷移を持つべきだ。`Phase2: INACTIVE` のような明示が必要。 `src/lisp/school/school-narrative.lisp:289-324`

## 🚀 Musk's Decision (Final)
> 「**Phase2は今すぐ意思決定する。** 使うなら実運用へ接続。使わないならレポートから削除し“Inactive”と明記する。」

## Actionable Items
1. Phase2(V2)を **実運用に接続するか、Reportから除去/Inactive表示にするか** を決定し反映。 `src/lisp/school/school-backtest-v2.lisp:100-105` `src/lisp/school/school-narrative.lisp:289-324`
2. Phase2を使う場合、**呼び出し元を明示的に追加**（例: 検証パイプラインで `run-phase-2-validation` を発火）。 `src/lisp/school/school-validation.lisp:276-299` `src/lisp/school/school-backtest-v2.lisp:100-105`
3. **Phase2 end_time の永続化 or 明示的無効化** を実装し、再起動で状態が消えないようにする。 `src/lisp/core/globals.lisp:112-113` `src/lisp/school/school-narrative.lisp:289-324`
4. **統合テスト**で Phase2 実行→ end_time 更新を保証する。 `src/lisp/tests/school-split-tests.lisp:461-476`
5. **仕様書と報告の整合**を取る（Phase2が無いならドキュメントからも削除）。 `doc/owners_guide.md:166-181`
